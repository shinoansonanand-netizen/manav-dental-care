runtime: nodejs20
env: standard

handlers:
  - url: /.*
    script: auto
    secure: always

# Ignore node_modules when uploading the source code
skip_files:
  - ^node_modules/.*
  - ^\.git/.*
  - ^\.next/.*
